import 'package:flutter/material.dart';
import 'components/myHome.dart';

void main() {
    runApp(MyApp());
}

class MyApp extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return MyHomeApp();
    }
}